namespace {
	// 画面幅(横)
	// int kScreenWidth = 0;
	//// 画面幅(縦)
	// int kScreenHeight = 0;

	 // 画面幅(横)
	 int kScreenWidth = 1920;
	 // 画面幅(縦)
	 int kScreenHeight = 1080;
	// カラービット数
	constexpr int kColorDepth = 16;
}